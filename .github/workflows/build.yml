name: AdBlock List Aggregator

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
permissions:
  contents: write # 授予默认 GITHUB_TOKEN 对仓库内容的写入权限，使其可以 push
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      # 1. 检出 main 分支
      - name: Checkout repository
        uses: actions/checkout@v4
        # 保持在 main 分支

      # 2. 设置 Go 环境
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24.0' 

      # 3. 构建并运行 Go 程序
      - name: Build and Run Go Program
        run: |
          go build -o adblock-aggregator main.go
          ./adblock-aggregator
          # 生成 adblock_aggr_optimized.txt 和 adblock_debug_unrecognized.txt

      # 4. 提交更改到 dist 分支 (KISS 原则优化)
      - name: Commit and Push new list to dist
        run: |
          # 1. 配置 Git 用户信息
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          
          # 2. 强制追踪并添加新生成的文件
          git add -f adblock_aggr_optimized.txt adblock_debug_unrecognized.txt
          
          # 3. 检查是否有实际的更改（防止空提交）
          if ! git diff --cached --exit-code --quiet; then
              echo "发现文件存在更改，开始提交并推送到 dist 分支..."
              
              # 4. 提交更改到工作区的暂存区
              git commit -m "🤖 Feat: Update aggregated adblock list (Daily Run)"
              
              # 5. 推送当前最新的提交到 'dist' 分支
              # 这里的 HEAD 指的是上一步创建的提交。
              # 强制推送 (+HEAD) 是因为 dist 分支的历史记录可能会与 main 分支的提交历史脱离，
              # 但这确保了 dist 分支始终是最新的构建结果。
              git push origin HEAD:dist
              
              # 注意：这里使用了简化的 git push 语法，利用了 GitHub Actions 的 GITHUB_TOKEN 权限。
          else
              echo "文件内容与上次提交相同，跳过提交。"
          fi