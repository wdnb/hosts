name: AdBlock List Aggregator

# 1. 设置触发器: 每天定时运行一次
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      # 1. 检出代码
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. 设置 Go 环境
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22' 

      # 3. 构建并运行 Go 程序
      - name: Build and Run Go Program
        run: |
          go build -o adblock-aggregator main.go
          ./adblock-aggregator
          # 运行后将生成 adblock_aggr_optimized.txt 文件

# 4. 提交更改回仓库
      - name: Commit and Push new list
        run: |
          # 1. 配置 Git 用户信息
          git config --global user.name 'github-actions[bot]'
          git config --global user.email '41898282+github-actions[bot]@users.noreply.github.com'
          
          # 2. 追踪并添加新生成的文件
          # 使用 git add -A 添加所有更改（包括新建文件），但只关注我们生成的 hosts 文件
          git add adblock_aggr_optimized.txt
          
          # 3. 检查是否有实际的更改（防止空提交）
          if ! git diff --cached --exit-code --quiet; then
              echo "发现 adblock_aggr_optimized.txt 存在更改，开始提交..."
              
              # 4. 提交更改
              git commit -m "🤖 Feat: Update aggregated adblock list (Daily Run)"
              
              # 5. 推送更改到 GitHub
              # 使用 GITHUB_TOKEN 认证进行推送
              git push
          else
              echo "文件 adblock_aggr_optimized.txt 内容与上次提交相同，跳过提交。"
          fi