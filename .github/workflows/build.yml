name: AdBlock List Aggregator

# 1. 设置触发器: 每天定时运行一次
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      # 1. 检出代码
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. 设置 Go 环境
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22' 

      # 3. 构建并运行 Go 程序
      - name: Build and Run Go Program
        run: |
          go build -o adblock-aggregator main.go
          ./adblock-aggregator
          # 运行后将生成 adblock_aggr_optimized.txt 文件

      # 4. 提交更改回仓库
      - name: Commit and Push new list
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          # 明确告诉 Git 追踪所有更改（包括新的、修改的、删除的）
          commit_options: '--all' 
          # 确保只提交这个文件
          file_pattern: 'adblock_aggr_optimized.txt'
          commit_message: "🤖 Feat: Update aggregated adblock list (Daily Run)"
          commit_user_name: 'github-actions[bot]'
          commit_user_email: 'github-actions[bot]@users.noreply.github.com'
          commit_author: 'GitHub Actions <41898282+github-actions[bot]@users.noreply.github.com>'